version: "3.8"

services:
  qwen3-asr-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: qwen3-asr-server:latest
    container_name: qwen3-asr-api
    restart: always
    ports:
      - "9001:9001"
    environment:
      - MODEL_NAME=Qwen/Qwen3-ASR-0.6B
      - DEVICE=cuda
      - TORCH_DTYPE=bfloat16
      - PORT=9001
    volumes:
      # Mount cache so weights are not re-downloaded on container restart
      - hf-cache:/root/.cache/huggingface
      # Mount adapters directory for multi-profile LoRA weights
      - ./adapters:/app/adapters
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

volumes:
  hf-cache:
    name: qwen3_asr_hf_cache
